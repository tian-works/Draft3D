numpy>=1.24
requests>=2.28

# Qt GUI (primary)
PySide6>=6.5

# Optional fallback Qt binding (GUI.py will use it if PySide6 is missing)
PyQt5>=5.15

# Computer vision utilities (used for image operations / future extensions)
opencv-python>=4.8

# 3D visualization stack
pyvista>=0.43
pyvistaqt>=0.11
vtk>=9.2

# Image processing & background removal (used by draft3d.operations)
pillow>=9.5
rembg>=2.0

# If you install the project in editable mode, the local packages
# (draft3d, draft3d_gui) will be installed from src/ via `pip install -e .`
# and do NOT need to be listed here.


